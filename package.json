{
  "name": "zoommarket-subquery",
  "version": "1.0.0",
  "description": "Moonbase ZOOM NFT Market",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Ryan Price",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "latest",
    "typescript": "^4.1.3",
    "moonbeam-types-bundle": "^2.0.2"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.5.0",
    "@subql/types": "latest",
    "ethers": "^5.5.0"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  }
}
